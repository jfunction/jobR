% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{jobr_serve}
\alias{jobr_serve}
\title{Serve jobs until the jobset is complete}
\usage{
jobr_serve(
  state,
  url,
  tls = NULL,
  timeout_ms = 1000,
  max_seconds = Inf,
  linger_seconds = 30,
  ready_file = NULL,
  quiet = FALSE
)
}
\arguments{
\item{state}{A \code{jobr_host} from \code{\link[=host_new]{host_new()}}.}

\item{url}{URL to listen on, e.g. \code{"tcp://0.0.0.0:5555"}.}

\item{tls}{Optional \code{tlsConfig} from \code{\link[=host_credentials]{host_credentials()}}.}

\item{timeout_ms}{How long to block waiting for each request.}

\item{max_seconds}{Stop after this long regardless of progress.}

\item{linger_seconds}{How long to keep answering after the last chunk is
done, so that workers can learn the jobset finished instead of inferring
it from silence. The host stops as soon as every enrolled worker has been
told, so a normal run does not wait this out; it is a cap for workers
that are not coming back. Set it to 0 to shut down the instant the work is
complete, at the cost of leaving any remaining worker to infer that from
silence.}

\item{ready_file}{Optional path written once the socket is actually bound,
and removed on exit. Supervisors and tests need an observable readiness
signal: dialling cannot provide one, because nanonext connects
asynchronously and a dial to an address with nothing behind it succeeds
just as readily as a dial to a live host.}

\item{quiet}{Suppress progress output.}
}
\value{
The host state, invisibly.
}
\description{
Serve jobs until the jobset is complete
}
