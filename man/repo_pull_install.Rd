% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{repo_pull_install}
\alias{repo_pull_install}
\title{Fetch a host's package repository and install from it}
\usage{
repo_pull_install(
  call,
  packages,
  dir,
  token = NULL,
  lib = .libPaths()[1],
  quiet = FALSE
)
}
\arguments{
\item{call}{A function taking a request list and returning the host's reply,
as \code{\link[=jobr_join]{jobr_join()}} uses internally.}

\item{packages}{Packages still needed.}

\item{dir}{Where to assemble the local copy. Kept between joins, so a second
visit transfers nothing.}

\item{token}{The session token from enrolment; the repository operations are
authenticated like every other one.}

\item{lib}{Library to install into.}

\item{quiet}{Suppress progress.}
}
\value{
The packages still missing afterwards.
}
\description{
Reassembles the host's repository locally, transferring only the files this
machine does not already have, then installs with \code{repos = "file:///..."} so
R's own resolution and version checking apply rather than anything invented
here.
}
