% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo.R
\name{host_serve_packages}
\alias{host_serve_packages}
\title{Stock a host's repository so workers need not visit CRAN}
\usage{
host_serve_packages(
  state,
  packages = NULL,
  types = c("source", .Platform$pkgType),
  series = r_series(),
  repos = getOption("repos"),
  quiet = FALSE
)
}
\arguments{
\item{state}{A \code{jobr_host}.}

\item{packages}{Packages to stock. Defaults to those the project's lockfile
declares.}

\item{types}{Kinds to stock. Sources work anywhere but need a toolchain;
binaries need none but must match the worker's platform and R series.}

\item{series}{R series to stock binaries for, defaulting to the host's own.
CRAN only builds binaries for the current R and the one before it.}

\item{repos}{CRAN mirror.}

\item{quiet}{Suppress progress.}
}
\value{
The host state, invisibly.
}
\description{
Downloads the project's declared packages, and everything they depend on,
once. Workers then fetch them over the same socket they use for work.
}
\details{
Call this after \code{\link[=host_new]{host_new()}} and before \code{\link[=jobr_serve]{jobr_serve()}}. It is deliberately
explicit rather than automatic: it reaches out to CRAN and can take a while,
which should never happen as a side effect of starting a host.
}
