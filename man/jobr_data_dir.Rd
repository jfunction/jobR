% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spool.R
\name{jobr_data_dir}
\alias{jobr_data_dir}
\title{Where jobR keeps data that must outlive the session}
\usage{
jobr_data_dir(what = NULL)
}
\arguments{
\item{what}{Subdirectory under the package's data directory.}
}
\value{
An absolute path. The directory is not created.
}
\description{
\code{tools::R_user_dir()} when the R in use has it, and the same platform
conventions by hand when it does not. R gained that function in 4.0, and
this package supports 3.6, which is exactly the vintage of machine most
likely to be donating cycles.
}
\details{
The fallback follows the conventions implemented by the \code{rappdirs} package
(MIT). \code{rappdirs} itself is not a dependency: it needs compilation, which
would put a toolchain in front of precisely the older Windows user this
package exists to include.
}
\keyword{internal}
