# R session / IDE state
.Rproj.user
.Rhistory
.RData
.Ruserdata

# renv is not used: DESCRIPTION declares dependencies directly, and renv
# belongs to projects rather than packages.
renv/
renv.lock

# build artefacts
*.tar.gz
*.Rcheck/

# superseded scratch from the 2022 prototype. Their intent is recorded in
# DESIGN.md; the code is in git history and in R/enrol.R.
misc/
sampleSession.R

# unrelated local files
server.sqlite
*.xlsx
~$*.xlsx
